# Server Configuration
HOST=0.0.0.0
PORT=8000
RELOAD=false

# Gaze Tracking (라즈베리파이 최적화)
CAMERA_INDEX=0
MODEL_NAME=ridge          # Ridge: 가볍고 빠름 (다른 옵션: elastic_net, svr, tiny_mlp)
FILTER_METHOD=noop        # NoOp: 필터링 없음, CPU 부하 최소화 (다른 옵션: kalman, kde)
SCREEN_WIDTH=1920
SCREEN_HEIGHT=1080

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Smart Home Integration (optional)
HOME_ASSISTANT_URL=
HOME_ASSISTANT_TOKEN=
MQTT_BROKER=
MQTT_PORT=1883

# 모델 선택 가이드 (라즈베리파이 성능 고려):
# ✅ ridge (권장): 선형 회귀, 가볍고 빠름
# ✅ elastic_net: L1+L2 정규화, 약간 더 정확
# ⚠️  svr: SVM 기반, 계산 더 필요
# ❌ tiny_mlp: 신경망, 가장 무거움

# 필터 선택 가이드 (라즈베리파이 성능 고려):
# ✅ noop (권장): 필터링 없음, 가장 빠름
# ⚠️  kde: 커널 밀도 추정, 메모리 사용
# ❌ kalman: 상태 예측, 가장 무거움
