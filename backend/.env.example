# ===== 라즈베리파이 4 & 7inch 디스플레이 설정 =====

# === 서버 설정 ===
HOST=0.0.0.0
PORT=8000               # 표준 포트
RELOAD=false            # 프로덕션: 디버그 모드 비활성화

# === Gaze Tracking 설정 (라즈베리파이 4 최적화) ===
CAMERA_INDEX=0
# 모델 선택 (라즈베리파이 4 CPU 부하 고려):
# ✅ ridge (권장): 선형 회귀, 최소 CPU/메모리, 매우 빠름 (~50ms)
# ✅ elastic_net: L1+L2 정규화, 약간 더 정확 (~60ms)
# ⚠️  svr: SVM 기반, 높은 정확도 (~100ms) - RPi4에는 무거움
# ❌ tiny_mlp: 신경망, 최고 정확도 (~200ms) - RPi4에는 너무 무거움
MODEL_NAME=ridge
# 필터 선택 (라즈베리파이 4 성능 고려):
# ✅ noop (권장): 필터링 없음, 매우 빠름, 최소 메모리
# ⚠️  kde: 커널 밀도 추정, 약간의 오버헤드 (~10ms 추가)
# ❌ kalman: 칼만 필터, 상태 예측, 더 많은 오버헤드 (~20ms 추가)
FILTER_METHOD=noop

# === 디스플레이 설정 (7inch 1024x600 → 800x480 최적화) ===
# 7inch 디스플레이 표준 해상도: 800x480
# - 더 작은 해상도 = 더 빠른 렌더링, 더 적은 메모리
# - 더 큰 터치 타겟 = 시선 제어에 최적
SCREEN_WIDTH=800
SCREEN_HEIGHT=480

# === CORS Origins (comma-separated) ===
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173,http://raspberrypi.local:3000,http://raspberrypi.local:5173,http://raspberrypi

# === AI 서버 설정 (AWS EC2) ===
# AWS 인스턴스: http://34.227.8.172:8000
AI_SERVER_URL=http://34.227.8.172:8000
# 라즈베리파이 네트워크 최적화:
# - 응답 대기 시간: 10초 (인터넷 연결 불안정 대비)
# - 최대 재시도: 3회 (네트워크 일시적 실패 대비)
AI_REQUEST_TIMEOUT=10
AI_MAX_RETRIES=3

# === MQTT 브로커 설정 ===
MQTT_BROKER=localhost
MQTT_PORT=1883

# === 성능 최적화 가이드 ===
# 라즈베리파이 4 사양: ARM Cortex-A72 (4x 1.5GHz), RAM 2-8GB
# 
# 추천 설정:
# - 모델: ridge (선형 회귀)
# - 필터: noop (필터링 비활성화)
# - 화면: 800x480 (7inch 디스플레이)
# - DWELL_TIME: 2000ms (시선 유지 시간)
#
# 성능 측정:
# - ridge 모델 추론: ~50ms
# - 프레임 처리: ~33ms (30 FPS)
# - 총 레이턴시: ~83ms (매우 반응적)
